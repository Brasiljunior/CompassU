'use client';

import {useEffect,useState} from 'react';
const SUPABASE_URL=process.env.NEXT_PUBLIC_SUPABASE_URL;const SUPABASE_KEY=process.env.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY;
const readSession=()=>{try{return JSON.parse(localStorage.getItem('compassu_session')||'null')}catch{return null}};
export default function AdminMonthlySendNowPanel(){const[session,setSession]=useState(null),[profiles,setProfiles]=useState([]),[institution,setInstitution]=useState(''),[busy,setBusy]=useState(false),[error,setError]=useState(''),[notice,setNotice]=useState('');const headers=s=>({apikey:SUPABASE_KEY,'Content-Type':'application/json',Authorization:`Bearer ${s?.access_token||''}`});async function load(s){try{const r=await fetch(`${SUPABASE_URL}/rest/v1/rpc/get_monthly_reporting_profiles`,{method:'POST',headers:headers(s),body:'{}'}),b=await r.json();if(!r.ok)throw new Error(b?.message||'Unable to load monthly reporting profiles.');const active=(b||[]).filter(x=>x.active!==false);setProfiles(active);setInstitution(v=>v||active[0]?.institution||'')}catch(e){setError(e.message)}}useEffect(()=>{const s=readSession();if(s?.access_token){setSession(s);load(s)}},[]);async function sendNow(){if(!session?.access_token||!institution||busy)return;if(!window.confirm(`Generate and email the previous month's configured CompassU reports for ${institution} now?`))return;setBusy(true);setError('');setNotice('');try{const r=await fetch('/api/admin/monthly-reports/send',{method:'POST',headers:{'Content-Type':'application/json',Authorization:`Bearer ${session.access_token}`},body:JSON.stringify({institution})}),b=await r.json();if(!r.ok)throw new Error(b?.error||'Unable to send monthly reports.');setNotice(`Sent ${b.reports?.length||0} monthly report(s) for ${b.period?.label||'the previous month'} to ${(b.recipients||[]).join(', ')}.`)}catch(e){setError(e.message)}finally{setBusy(false)}}if(!session)return null;return <section className="analyticsPanel adminPanel"><div className="analyticsHead"><div><div className="adminKicker">STAGE 2E · DELIVERY TEST</div><h2>Send Monthly Reports Now</h2><p>Generate the previous completed calendar month's configured PDF package and email it immediately to the institution's saved recipients.</p></div></div>{error&&<div className="error adminNotice">{error}</div>}{notice&&<div className="success adminNotice">{notice}</div>}<div className="analyticsCard"><label>Institution</label><select value={institution} onChange={e=>setInstitution(e.target.value)}><option value="">Select institution</option>{profiles.map(p=><option key={p.id} value={p.institution}>{p.institution}</option>)}</select><div className="adminPanelActions" style={{marginTop:14}}><button className="btn primary" disabled={busy||!institution} onClick={sendNow}>{busy?'Generating and sending…':'Send Previous Month Reports Now'}</button><button className="btn ghost" disabled={busy} onClick={()=>load(session)}>Refresh Profiles</button></div><p className="analyticsSub" style={{marginTop:12}}>This control uses the saved report selections and recipient list. It does not grant customer administrator access.</p></div></section>}
