'use client';

import {useEffect} from 'react';
const URL=process.env.NEXT_PUBLIC_SUPABASE_URL,KEY=process.env.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY,pct=v=>`${Number(v||0).toFixed(0)}%`;
const describe=(name,rows=[])=>{const r=(Array.isArray(rows)?rows:[]).filter(x=>x?.trait_name&&x?.user_score!=null).sort((a,b)=>Number(b.user_score)-Number(a.user_score)).slice(0,2);if(r.length>=2)return `Your responses show especially strong alignment in ${r[0].trait_name} (${pct(r[0].user_score)}) and ${r[1].trait_name} (${pct(r[1].user_score)}). These patterns help explain why ${name} is one of your recommended directions and make it worth exploring through courses, careers, and college programs.`;if(r.length===1)return `Your responses show strong alignment in ${r[0].trait_name} (${pct(r[0].user_score)}), which helps explain why ${name} appears among your recommended directions. Explore its coursework and career options to see how well it fits your goals.`;return `${name} is one of your strongest CompassU recommendations based on patterns across your assessment responses. Use this match as a starting point to explore coursework, related careers, and college options.`};
export default function MajorDescriptions(){useEffect(()=>{let stopped=false,timer=null;const run=async()=>{try{const session=JSON.parse(localStorage.getItem('compassu_session')||'null');if(!session?.access_token||!session?.user?.id||!URL||!KEY)return;const headers={apikey:KEY,Authorization:`Bearer ${session.access_token}`,'Content-Type':'application/json'};const attempts=await fetch(`${URL}/rest/v1/assessment_attempts?user_id=eq.${session.user.id}&status=eq.completed&select=id&order=completed_at.desc&limit=1`,{headers}).then(r=>r.json()),aid=attempts?.[0]?.id;if(!aid)return;const matches=await fetch(`${URL}/rest/v1/major_matches?attempt_id=eq.${aid}&select=major_id,match_score,rank,majors(name)&order=rank.asc&limit=10`,{headers}).then(r=>r.json());if(!Array.isArray(matches))return;const map={};await Promise.all(matches.map(async m=>{try{const rows=await fetch(`${URL}/rest/v1/rpc/get_major_explanation`,{method:'POST',headers,body:JSON.stringify({p_attempt_id:aid,p_major_id:m.major_id})}).then(r=>r.json());map[m.majors?.name]=describe(m.majors?.name||'This major',rows)}catch{map[m.majors?.name]=describe(m.majors?.name||'This major',[])}}));if(stopped)return;document.querySelectorAll('.match .matchMain').forEach(btn=>{const text=btn.querySelector('div:nth-child(2) b')?.textContent?.trim(),host=btn.querySelector('div:nth-child(2)');if(!text||!host||host.querySelector('.personalizedMajorDescription'))return;const p=document.createElement('div');p.className='personalizedMajorDescription';p.textContent=map[text]||describe(text,[]);Object.assign(p.style,{fontSize:'12px',lineHeight:'1.45',color:'#667085',marginTop:'7px',maxWidth:'520px'});host.appendChild(p)})}catch{}};const observer=new MutationObserver(()=>{clearTimeout(timer);timer=setTimeout(run,120)});observer.observe(document.body,{childList:true,subtree:true});run();return()=>{stopped=true;clearTimeout(timer);observer.disconnect()}},[]);return null}
